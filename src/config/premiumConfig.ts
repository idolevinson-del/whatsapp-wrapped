/**
 * TODO: replace with the real LemonSqueezy checkout link once the store +
 * product (one-time payment, License Keys enabled) exist. Format is
 * `https://<store>.lemonsqueezy.com/buy/<variant-id>`.
 */
export const CHECKOUT_URL = 'https://whatsapp-wrapped.lemonsqueezy.com/buy/REPLACE_WITH_VARIANT_ID';
